<section class="auth">
    <div class="card">
        <h1>Connexion</h1>
        <p class="auth-subtitle">Content de te revoir !</p>

        <?php if (isset($_SESSION["error-message"])) : ?>
            <p class="error-message"><?= htmlspecialchars($_SESSION["error-message"]) ?></p>
            <?php unset($_SESSION["error-message"]); ?>
        <?php endif; ?>

        <form action="index.php?route=check-login" method="post">
            <input type="hidden" name="csrf_token" value="<?= htmlspecialchars($_SESSION["csrf_token"]) ?>">

            <fieldset>
                <label for="email">Adresse email</label>
                <input type="email" name="email" id="email" placeholder="sarah@email.com" required>
            </fieldset>

            <fieldset>
                <label for="password">Mot de passe</label>
                <input type="password" name="password" id="password" placeholder="Ton mot de passe" required>
            </fieldset>

            <button type="submit" class="btn btn-primary btn-full">Se connecter</button>
        </form>

        <p class="auth-link">Pas encore de compte ? <a href="index.php?route=register">Inscris-toi</a></p>
    </div>
</section>
